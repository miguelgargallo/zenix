{
  "name": "zenix",
  "description": "High-quality, remote control, cutting-edge framework and modern blog starter pack.",
  "type": "module",
  "license": "Pylar AI creative ML license",
  "author": "Miguel Gargallo",
  "version": "0.0.45",
  "github version": "0.0.45-115",
  "npm version": "0.0.45",
  "private": false,
  "scripts": {
    "db": "cd .. && cd backend && ./pocketbase serve",
    "zenix": "astro dev",
    "start-zenix": "astro dev",
    "build-zenix": "astro build",
    "preview-zenix": "astro preview",
    "rocket-zenix": "npm publish",
    "clean-zenix": "rm -r node_modules",
    "bye-zenix": "rm -r node_modules && npm i",
    "clean-dev-zenix": "rm -r node_modules && npm i && npm run zenix",
    "clean-build-zenix": "rm -r node_modules && npm i && npm run build-zenix",
    "sudo-clean-zenix": "sudo rm -r node_modules",
    "sudo-bye-zenix": "sudo rm -r node_modules && npm i",
    "sudo-clean-dev-zenix": "sudo rm -r node_modules && npm i && npm run zenix",
    "sudo-clean-build-zenix": "sudo rm -r node_modules && npm i && npm run build-zenix",
    "astro-zenix": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^0.19.0",
    "@astrojs/rss": "^2.3.2",
    "@astrojs/sitemap": "^1.2.2",
    "astro": "2.3.0"
  }
}